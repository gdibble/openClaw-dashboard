# Path to your OpenClaw swarm's tasks directory
OPENCLAW_TASKS_DIR=./tasks

# Optional: API key for protecting the dashboard endpoint
# If not set, the API is open (suitable for local use)
OPENCLAW_API_KEY=

# Optional: API key for client-side auth (must match OPENCLAW_API_KEY)
NEXT_PUBLIC_OPENCLAW_API_KEY=

# PostgreSQL connection — enables DB mode (task persistence, auth, chat, routines)
# If unset, dashboard falls back to file-based mode (v1 behavior)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Dashboard operator password — enables login protection
# If unset, dashboard has open access (suitable for local use)
# DASHBOARD_SECRET=

# Anthropic API key for agent chat (Phase 6)
# ANTHROPIC_API_KEY=
