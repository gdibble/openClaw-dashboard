# ── File-based mode ───────────────────────────────────────────────────
# Path to your OpenClaw swarm's tasks directory (file-based fallback)
OPENCLAW_TASKS_DIR=./tasks

# ── Database mode (optional — enables full v2.0 features) ────────────
# PostgreSQL connection string — enables task persistence, auth, chat, routines
# If unset, dashboard falls back to file-based mode (v1 behavior)
# DATABASE_URL=postgresql://user:password@localhost:5432/openclaw_dashboard

# ── Auth (optional — enables login protection) ───────────────────────
# Dashboard operator password + JWT signing secret
# If unset, dashboard has open access (suitable for local use)
# DASHBOARD_SECRET=

# ── Gateway connection (optional — for live cluster integration) ─────
# The dashboard depends on the gateway — an unreachable gateway causes 502 errors.
# WebSocket RPC endpoint for the OpenClaw gateway
# GATEWAY_WS_URL=ws://127.0.0.1:18789
# Gateway authentication token
# GATEWAY_TOKEN=
# Gateway health check endpoint (HTTP)
# GATEWAY_HEALTH_URL=http://127.0.0.1:18792
# Dashboard origin sent as WS header (default: http://localhost:$PORT)
# Must match gateway's controlUi.allowedOrigins
# GATEWAY_ORIGIN=http://192.168.1.100:3000
# Client ID sent during gateway handshake (default: openclaw-control-ui)
# Must be in gateway's allowed list — see AGENTS.md for valid values
# GATEWAY_CLIENT_ID=openclaw-control-ui
# Role for gateway auth (default: operator)
# GATEWAY_ROLE=operator
# Comma-separated scopes to request (default: operator.admin,operator.read,operator.write,operator.talk)
# GATEWAY_SCOPES=operator.admin,operator.read,operator.write,operator.talk

# ── Agent chat (optional) ────────────────────────────────────────────
# Anthropic API key for agent chat responses
# ANTHROPIC_API_KEY=
