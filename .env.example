# Path to your OpenClaw swarm's tasks directory
OPENCLAW_TASKS_DIR=./tasks

# Optional: API key for protecting the dashboard endpoint
# If not set, the API is open (suitable for local use)
OPENCLAW_API_KEY=

# Optional: API key for client-side auth (must match OPENCLAW_API_KEY)
NEXT_PUBLIC_OPENCLAW_API_KEY=

# Optional: Custom dashboard name (defaults to "OpenClaw")
NEXT_PUBLIC_DASHBOARD_NAME=

# Optional: Repository URL — shows a link icon in the header
NEXT_PUBLIC_REPO_URL=

# PostgreSQL connection for bridge script (sync-db.ts)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
